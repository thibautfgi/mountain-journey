{
  "name": "mountain-journey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "front:install": "cd mj-front && npm install",
    "front:start": "cd mj-front && npm start",
    "front:build": "cd mj-front && npm run build",
    "front:test": "cd mj-front&& npm test",
    "moteur:install": "cd mj-moteur && mvn clean install",
    "moteur:start": "cd mj-moteur && mvn spring-boot:run",
    "auth:install": "cd mj-auth && mvn clean install",
    "auth:start": "cd mj-auth && mvn spring-boot:run",
    "auth:test": "cd mj-auth && mvn test",
    "moteur:test": "cd mj-moteur && mvn test",
    "install": "npm run front:install && npm run moteur:install && npm run auth:install",
    "start": "concurrently \"npm run moteur:start\" \"npm run auth:start\" \"npm run front:start\"",
    "build": "npm run front:build && npm run moteur:install",
    "prettier": "prettier --write .",
    "submodule:init": "git submodule update --init --recursive",
    "submodule:update": "git submodule update --remote --merge",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down -v",
    "docker:logs": "docker compose logs -f",
    "k6:local": "node k6/run-k6.js local",
    "k6:prod": "node k6/run-k6.js prod"
  },
  "devDependencies": {
    "@apideck/postman-to-k6": "^1.14.1",
    "concurrently": "^8.2.2",
    "prettier": "^3.8.1"
  }
}
